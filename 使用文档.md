# 使用文档

## 1. 项目概览

本系统用于管理员工基础信息，主要包括：

- 员工姓名
- 身份证号
- 银行账号
- 银行地址
- 电话号码

用户可以通过网页界面进行查询、编辑、删除以及 Excel 导出。

## 2. 启动项目

### 方式一：使用启动脚本（推荐）

在 Windows 环境下，直接双击运行：

- `启动项目.bat`：启动服务（自动定位项目目录、检查 Python 与依赖）
- `关闭项目.bat`：停止服务（只结束本系统进程，不影响其他 Python 程序）

### 方式二：命令行启动

```bash
python app.py
```

访问地址：

```text
http://127.0.0.1:5001
```

## 3. 登录说明

打开浏览器后，进入登录页面。首次使用前，请先创建管理员用户。

### 创建管理员用户

```bash
python gzb_admin.py add admin 123456 --admin
```

登录成功后即可进入主界面进行操作。

## 4. 功能操作说明

### 4.1 员工信息查询

在主页中可以：

- 按姓名搜索员工
- 按拼音首字母搜索员工
- 查看所有员工信息
- 员工列表采用分页显示，默认每页 20 条；可使用“上一页”和“下一页”浏览

### 4.2 员工信息新增与更新

新增或编辑员工信息时，需要填写：

- 姓名（必填）
- 身份证号
- 银行账号
- 银行地址
- 电话

如果姓名已存在，则会执行更新；否则会新增记录。

### 4.3 删除员工信息

删除是**软删除**：记录不会立即消失，而是进入回收站，查询、列表和导出中都不再显示。

### 4.4 回收站

点击主页右上角的"🗑️ 回收站"进入，可以：

- 查看所有已删除的员工记录，以及**删除时间**和**删除人**
- 回收站采用分页显示，默认每页 20 条，并显示总记录数
- 点击"恢复"将记录还原到在职员工列表
- 管理员可点击"彻底删除"永久清除记录（不可恢复，需二次确认）

另外，在"录入/更新"中重新添加同名员工时，如果该姓名在回收站中，会自动恢复并更新为新内容。

### 4.5 Excel 上传导入

在主页"📥 导入Excel"标签页中：

1. 选择 `.xlsx` 文件（表头支持中英文：姓名/real_name、身份证号/id_number 等）
2. 点击"上传并预览"，系统生成**校验报告**：逐行标注 新增/更新/恢复/错误，并提示身份证号位数异常、电话含非数字、姓名重复等问题
3. 确认无误后点击"确认导入"，错误行会自动跳过

也可以在命令行导入：`python gzb_update.py data.xlsx`

### 4.6 Excel 导出

#### 自动导出模式

1. 将姓名清单准备到 `input.xlsx`
2. 调用导出接口
3. 系统会生成 `output.xlsx`

#### 手动导出模式

1. 在前端手动输入姓名列表
2. 系统会生成 `shuchu.xlsx`

### 4.7 操作日志（仅管理员）

管理员登录后，主页会出现"📋 操作日志"标签页，可查看系统的操作记录。日志分两类：

- **🖥️ 系统日志**：登录/登出、Excel 导出、文件下载、HTTP 请求、服务异常等
- **🗄️ 数据库改动日志**：员工新增/更新/删除/恢复/彻底删除、Excel 导入写入、用户创建/删除/重置密码

页面顶部可在两类日志间切换，每类均支持：

- **日期筛选**：日志按天保存（默认展示当天，保留最近 30 天），可切换历史日期
- **级别筛选**：INFO（正常操作）/ WARNING（需注意，如彻底删除）/ ERROR（错误）
- **关键词搜索**：输入姓名或操作关键词，实时过滤
- **分页浏览**：每页 50 条，最新记录在前

日志文件保存在项目目录的 `logs/` 文件夹中（`system.log` / `database.log` 为当天，历史为 `*.log.YYYY-MM-DD`），可直接用文本编辑器查看。

## 5. 管理员功能

管理员可以：

- 查看所有用户
- 创建普通用户或管理员用户
- 删除用户
- 重置用户密码
- 在回收站中彻底删除员工记录（普通用户只能恢复，不能彻底删除）
- 在"操作日志"标签页查看系统操作记录

## 6. 常见问题

### 6.1 启动报错：找不到模块

请先安装依赖：

```bash
pip install -r requirements.txt
```

### 6.2 登录失败

请确认：

- 账号是否存在
- 密码是否正确
- 用户数据库 `sjk.db` 是否已初始化

### 6.3 导出失败

请确认：

- `input.xlsx` 是否存在
- Excel 文件中是否有姓名数据
- `openpyxl` 是否已安装

## 7. 数据备份

系统默认不会在启动时自动备份。管理员可以进入“管理后台”，在用户列表下方找到“数据库备份”组件：

1. 点击“立即备份”创建数据库备份。
2. 备份成功或失败时，页面右上角会弹窗提示。
3. 点击“查看备份”进入备份子页面，查看备份文件名、创建时间和大小。
4. 点击“下载”将选定的备份文件保存到本地。

备份文件默认保存在项目目录的 `backups/` 文件夹中，默认保留最近 10 个备份。也可以在项目目录执行手动备份：

```bash
python gzb_backup.py
```

常用配置：

- `GZD_BACKUP_DIR`：修改备份目录
- `GZD_BACKUP_RETENTION`：修改保留备份数量，默认 10

## 8. 文件说明

- `input.xlsx`：导出依赖的输入表
- `output.xlsx`：程序导出的结果文件
- `shuchu.xlsx`：手工导出的结果文件
- `sjk.db`：员工信息与用户信息数据库
- `backups/`：数据库手动备份目录

## 9. 安全提醒

- 定期将 `backups/` 目录复制到其他磁盘或可靠的远程存储，避免设备损坏导致备份与原库同时丢失
- 不要在代码中明文存储密码
- 下线后请退出登录，避免账号被他人误用
