# 使用文档

## 1. 项目概览

本系统用于管理员工基础信息，主要包括：

- 员工姓名
- 身份证号
- 银行账号
- 银行地址
- 电话号码

用户可以通过网页界面进行查询、编辑、删除以及 Excel 导出。

## 2. 启动项目

### 方式一：命令行启动

```bash
python -c "from app import app; app.run(port=5001, debug=True, host='0.0.0.0')"
```

### 方式二：使用启动脚本

在 Windows 环境下，可以直接双击运行：

- `启动项目.bat`

访问地址：

```text
http://127.0.0.1:5001
```

## 3. 登录说明

打开浏览器后，进入登录页面。首次使用前，请先创建管理员用户。

### 创建管理员用户

```bash
python gzb_admin.py add admin 123456 --admin
```

登录成功后即可进入主界面进行操作。

## 4. 功能操作说明

### 4.1 员工信息查询

在主页中可以：

- 按姓名搜索员工
- 按拼音首字母搜索员工
- 查看所有员工信息

### 4.2 员工信息新增与更新

新增或编辑员工信息时，需要填写：

- 姓名（必填）
- 身份证号
- 银行账号
- 银行地址
- 电话

如果姓名已存在，则会执行更新；否则会新增记录。

### 4.3 删除员工信息

可通过删除操作删除一条员工记录。

### 4.4 Excel 导出

#### 自动导出模式

1. 将姓名清单准备到 `input.xlsx`
2. 调用导出接口
3. 系统会生成 `output.xlsx`

#### 手动导出模式

1. 在前端手动输入姓名列表
2. 系统会生成 `shuchu.xlsx`

## 5. 管理员功能

管理员可以：

- 查看所有用户
- 创建普通用户或管理员用户
- 删除用户
- 重置用户密码

## 6. 常见问题

### 6.1 启动报错：找不到模块

请先安装依赖：

```bash
pip install flask pandas openpyxl pypinyin Werkzeug
```

### 6.2 登录失败

请确认：

- 账号是否存在
- 密码是否正确
- 用户数据库 `sjk.db` 是否已初始化

### 6.3 导出失败

请确认：

- `input.xlsx` 是否存在
- Excel 文件中是否有姓名数据
- `openpyxl` 是否已安装

## 7. 文件说明

- `input.xlsx`：导出依赖的输入表
- `output.xlsx`：程序导出的结果文件
- `shuchu.xlsx`：手工导出的结果文件
- `sjk.db`：员工信息与用户信息数据库

## 8. 安全提醒

- 建议定期备份 `sjk.db` 和 Excel 文件
- 不要在代码中明文存储密码
- 下线后请退出登录，避免账号被他人误用
