# 项目常用终端命令

## 进入项目目录
```bash
cd /e/code/c/gongzidan
```

## 启动项目
由于当前使用 5001 端口更稳定，推荐使用：
```bash
cd /e/code/c/gongzidan && /e/anaconda1/python -c "from app import app; app.run(port=5001, debug=True)"
```

启动后在浏览器打开：
```text
http://127.0.0.1:5001
```

## 按默认方式启动
如果以后 5000 端口恢复正常，也可以直接运行：
```bash
cd /e/code/c/gongzidan && /e/anaconda1/python app.py
```

默认访问地址：
```text
http://127.0.0.1:5000
```

## 关闭项目
在正在运行 Flask 的终端窗口中按：
```text
Ctrl + C
```

## 查看 5000/5001 端口是否被占用
```bash
netstat -ano | grep 5000
```

```bash
netstat -ano | grep 5001
```

## 强制关闭占用端口的 Python 进程
```bash
taskkill //F //IM python.exe
```

## 安装依赖
如果缺少模块，可以安装：
```bash
/e/anaconda1/python -m pip install flask pandas openpyxl pypinyin
```

## 重新启动项目的推荐步骤
```bash
cd /e/code/c/gongzidan
/e/anaconda1/python -c "from app import app; app.run(port=5001, debug=True)"
```
怎么关闭
有两种方式：

在这个 bat 打开的黑窗口里按 Ctrl + C
如果没反应，就直接把这个黑窗口关掉
如果还有残留进程，也可以在终端里执行：


taskkill //F //IM python.exe