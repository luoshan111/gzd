# 开发文档

## 1. 项目定位

本项目是一个轻量级的员工信息管理 Web 应用，适合做内部管理工具、信息登记系统、员工资料查询系统等场景。

## 2. 架构概览

### 2.1 技术架构

- Web 层：Flask
- 数据层：SQLite
- 数据处理：pandas + openpyxl
- 搜索能力：拼音首字母辅助检索
- 身份认证：Session + Werkzeug 密码哈希

### 2.2 核心模块

- `app.py`：Flask 主应用，路由、接口、鉴权
- `config.py`：集中配置（环境变量可覆盖）
- `db.py`：数据库访问层（连接、建表、迁移、查询助手）
- `excel_utils.py`：Excel 导入导出共享逻辑
- `gzb_admin.py`：CLI 管理脚本
- `gzb_find.py`：查询脚本
- `gzb_update.py`：数据导入脚本
- `gzb_output.py`：结果生成脚本
- `templates/`：页面模板（index / login / admin / recycle_bin）

## 3. 数据模型

数据库文件：`sjk.db`

### employees 表

字段说明：

- `real_name`：姓名，主键
- `id_number`：身份证号
- `bank_account`：银行账号
- `bank_address`：银行地址
- `phone`：电话号码
- `sx`：姓名拼音首字母，用于搜索
- `deleted`：软删除标记，0 在职 / 1 已删除
- `deleted_at`：删除时间（审计）
- `deleted_by`：删除操作人（审计）

索引：`idx_employees_deleted_sx ON employees(deleted, sx)`。
旧库启动时自动迁移补列（幂等），无需手工处理。

### users 表

字段说明：

- `id`：自增主键
- `username`：用户名，唯一
- `password_hash`：哈希后的密码
- `is_admin`：是否为管理员
- `created_at`：创建时间

## 4. 核心接口说明

### 登录接口

- `POST /api/login`

参数：

```json
{
  "username": "admin",
  "password": "123456"
}
```

### 员工查询接口

- `GET /api/search?name=张三`
- `GET /api/employees`

### 员工新增/更新接口

- `POST /api/employee`
- `PUT /api/employee`

### 员工删除接口

- `DELETE /api/employee/<name>`（软删除，记录删除时间与操作人）

### 回收站接口

- `GET /api/recycle-bin`：已删除记录列表（按删除时间倒序）
- `POST /api/employee/<name>/restore`：恢复记录
- `DELETE /api/employee/<name>/permanent`：彻底删除（仅管理员，仅限回收站中的记录）

### Excel 上传导入接口

- `POST /api/import-preview`：multipart 上传 `.xlsx`（≤5MB，内存解析不落盘），返回逐行校验报告（new/update/restore/error + messages）与分类统计
- `POST /api/import-confirm`：接收确认的行数据，服务端重新清洗分类后单事务写入

解析/校验/写入逻辑统一在 `excel_utils.py`（`read_employee_excel` / `classify_import_rows` / `import_employee_rows`），Web 接口与 `gzb_update.py` 共用。

### Excel 导出接口

- `POST /api/export`
- `POST /api/export-manual`

### 下载接口

- `GET /api/download/<filename>`

## 5. 关键实现点

### 5.1 登录与权限控制

项目通过装饰器实现：

- `login_required`
- `admin_required`

登录态使用 Flask 的 `session` 实现。

### 5.2 拼音首字母

通过 `pypinyin` 计算姓名的首字母，用于支持拼音搜索：

```python
lazy_pinyin(name, style=Style.FIRST_LETTER)
```

搜索接口对关键词中的 `%`、`_` 使用 `ESCAPE '\'` 转义，按字面匹配。

### 5.3 数据库访问

统一走 `db.py`：`query_all` / `query_one` / `execute` 助手自动管理连接；
`db_session(commit=True)` 上下文管理器用于多语句事务；`init_db()` 负责建表、索引与旧库幂等迁移。

### 5.4 软删除与回收站

员工删除为软删除（`deleted=1`），所有查询出口统一过滤 `deleted=0`；
回收站页面 `/recycle-bin` 支持恢复（清除标记）与彻底删除（物理删除，仅管理员）。

## 6. 开发环境建议

### 依赖安装

```bash
pip install flask pandas openpyxl pypinyin Werkzeug
```

### 运行方式

```bash
python app.py          # 或双击 启动项目.bat
```

## 7. 后续优化方向

已完成：配置环境变量化、统一 API 响应结构、搜索转义、软删除与回收站、Web 端 Excel 上传导入与校验。

建议后续逐步完善以下内容：

1. 分页与排序（员工量增长后）
2. 操作日志表（谁在何时改了什么）
3. 数据库自动备份
4. 将冒烟测试固化为 pytest 套件

## 8. 代码维护建议

- 优先保证数据库结构向后兼容
- 新增字段时同步更新：数据库建表、接口读取、导出逻辑
- 对 Excel 导出逻辑做单独测试和样例文件覆盖
- 当前项目以“可用为主”，后续可逐步抽离为更正式的服务端结构

## 9. 备注

这是一个“功能导向型”的小型项目，当前代码实现偏直接和聚合式。为了便于后续维护，建议后续把接口、服务层、数据库操作和模板封装得更清晰。
